mixin boxFilter(filterStatus)
    if filterStatus && filterStatus.length > 0
        each item in filterStatus
            button(class=`btn btn-sm ml-1 btn-outline-success ${item.class}`,button-status=item.status) #{item.name}